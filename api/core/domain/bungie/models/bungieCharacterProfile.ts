
// this interface will be used to map the data coming from the Bungie API.
export interface BungieCharacterProfile {
    characterId: string;
    classType: number;
    emblemPath: string;
    emblemBackgroundPath: string;
}